# Vendored dependencies inside pip/_vendor/ — not user-installed packages.
# pip ships its own internal copies of setuptools and msgpack for its own use.
# These are not exploitable via our application code.
CVE-2025-47273
GHSA-6v7p-g79w-8964